---
apiVersion: v1

# The repository name in registry1, excluding /ironbank/
name: "opensource/apache/apache2"

# List of tags to push for the repository in registry1
# The most specific version should be the first tag and will be shown
# on ironbank.dsop.io
tags:
- "2.4.37"
- "latest"

# Build args passed to Dockerfile ARGs
args:
  BASE_IMAGE: "redhat/ubi/ubi8-minimal"
  BASE_TAG: "8.5"

# Docker image labels
labels:
  org.opencontainers.image.title: "apache2"
  ## Human-readable description of the software packaged in the image
  org.opencontainers.image.description: "httpd is an open-source HTTP server for modern operating systems including UNIX and Windows."
  ## License(s) under which contained software is distributed
  org.opencontainers.image.licenses: "Apache License 2.0"
  ## URL to find more information on the image
  org.opencontainers.image.url: "https://downloads.apache.org/httpd/"
  ## Name of the distributing entity, organization or individual
  org.opencontainers.image.vendor: "The Apache Software Foundation"
  org.opencontainers.image.version: "2.4.37"
  ## Keywords to help with search (ex. "cicd,gitops,golang")
  mil.dso.ironbank.image.keywords: "Apache,httpd"
  ## This value can be "opensource" or "commercial"
  mil.dso.ironbank.image.type: "opensource"
  ## Product the image belongs to for grouping multiple images
  mil.dso.ironbank.product.name: "apache"

# List of resources to make available to the offline build context
resources: []

# List of project maintainers
maintainers:
- email: "melissari_sean@bah.com"
#   # The name of the current container owner
  name: "Sean Melissari"
#   # The gitlab username of the current container owner
  username: "sean.melissari"
  cht_member: true
